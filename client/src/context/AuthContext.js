import { createContext } from 'react';

// Create a new React Context for authentication
// This will be used to provide and consume auth data (like user, login, logout)
export const AuthContext = createContext();